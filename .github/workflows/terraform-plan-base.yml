name: Terraform

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test ${{inputs.environment}}"
