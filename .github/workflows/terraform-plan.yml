name: Plan
on: push
jobs:
  test:
    strategy:
      matrix:
        environment: [staging, production]
    uses: ./.github/workflows/terraform-plan-base.yml
    with:
      environment: ${{ matrix.environment }}
    secrets: inherit
